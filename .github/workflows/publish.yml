name: Publish to GitHub Packages

on:
  push:
    branches: [main]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com

      - run: pnpm install
      - run: pnpm build

      - name: Publish core
        working-directory: packages/core
        run: npm publish --access public || echo "Version already exists, skipping core"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish CLI
        working-directory: apps/cli
        run: npm publish --access public || echo "Version already exists, skipping cli"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
